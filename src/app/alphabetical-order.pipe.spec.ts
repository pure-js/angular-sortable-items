import { AlphabeticalOrderPipe } from './alphabetical-order.pipe';

describe('AlphabeticalOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabeticalOrderPipe();
    expect(pipe).toBeTruthy();
  });
});

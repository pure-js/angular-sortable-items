import { FlagsFilterPipe } from './flags-filter.pipe';

describe('FlagsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FlagsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
